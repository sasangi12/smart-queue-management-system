<!-- ==========================================
            ADMIN NAVBAR
========================================== -->

<nav class="top-navbar">

    <!-- Left Section -->

    <div class="navbar-left">

        <button class="menu-toggle">

            <i class="bi bi-list"></i>

        </button>

        <div>

            <h3>Admin Dashboard</h3>

            <p>System-wide overview of hospital operations.</p>

        </div>

    </div>

    <!-- Right Section -->

    <div class="navbar-right">

        <!-- Current Date -->

        <div class="current-date">

            <i class="bi bi-calendar-event"></i>

            <span id="currentDate"></span>

        </div>

        <!-- Notification -->

        <div class="notification">

            <a href="/admin/notifications">

                <i class="bi bi-bell-fill"></i>

                <% if (typeof unreadNotifications !== 'undefined' && unreadNotifications > 0) { %>

                    <span class="notification-count">

                        <%= unreadNotifications > 9 ? '9+' : unreadNotifications %>

                    </span>

                <% } %>

            </a>

        </div>

        <!-- Profile (icon + name only -- no dropdown) -->

        <div class="profile-dropdown" style="cursor:default;">

            <img
                src="<%= user.profile_image ? user.profile_image : '/images/default-user.png' %>"
                alt="Admin">

            <div class="profile-info">

                <h6>

                    <%= user.full_name %>

                </h6>

                <span>

                    Administrator

                </span>

            </div>

        </div>

    </div>

</nav>

<!-- Date Script -->

<script>

const today = new Date();

const options = {

    weekday: 'long',

    year: 'numeric',

    month: 'long',

    day: 'numeric'

};

document.getElementById("currentDate").innerHTML =

today.toLocaleDateString('en-US', options);

</script>
